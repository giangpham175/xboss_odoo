<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="salecommission.list">
      <field name="name">salecommission list</field>
      <field name="model">salecommission.salecommission</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="salecommission.action_window">
      <field name="name">salecommission window</field>
      <field name="res_model">salecommission.salecommission</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="salecommission.action_server">
      <field name="name">salecommission server</field>
      <field name="model_id" ref="model_salecommission_salecommission"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="salecommission" id="salecommission.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="salecommission.menu_1" parent="salecommission.menu_root"/>
    <menuitem name="Menu 2" id="salecommission.menu_2" parent="salecommission.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="salecommission.menu_1_list" parent="salecommission.menu_1"
              action="salecommission.action_window"/>
    <menuitem name="Server to list" id="salecommission" parent="salecommission.menu_2"
              action="salecommission.action_server"/>
    -->
  </data>
</odoo>